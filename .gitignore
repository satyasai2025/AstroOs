# Python
__pycache__/
*.pyc
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/

# Node / frontend
node_modules/
.next/
*.tsbuildinfo
dist/
build/
package-lock.json

# Environment / secrets
.env
apps/api/security/keys/*.pem

# Editor/OS
.DS_Store

# Junk files from broken Windows-style shell commands (e.g. `dir /s /b > nul`
# run in a POSIX shell — /s and /b get treated as extra args, nul becomes a
# real file instead of the null device). See DOCUMENTATION_REALITY_AUDIT.md §5.
nul
alembic
docker
echo
env.py
location
uvicorn

# Replit / agent session artifacts — not application code
attached_assets/
.agents/
.local/
.replit
replit.nix

# Local Claude configuration
.claude/settings.json

# Local archive files
apps.7z
apps.zip
packages.zip

# Temporary documentation
\## Docs & architecture.txt
New folder/
.venv/
apps/web/.env.local
