import { ExperimentStudio } from "@/components/research/ExperimentStudio";

export const metadata = {
  title: "Scientific Experiment Studio | AstroOS Research",
  description: "Immutable experiment lineage tracking, snapshot versioning, and comparative diff engine",
};

export default function ExperimentStudioPage() {
  return <ExperimentStudio />;
}
